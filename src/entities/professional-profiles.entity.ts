export type ProfessionalProfileEntity = {
  id: string;
  platform: string;
  url: string;
  memberId: string;
  createdAt: string;
};

export type HttpProfessionalProfileEntity = {
  id: string;
  platform: string;
  url: string;
  member_id: string;
  created_at: string;
};
