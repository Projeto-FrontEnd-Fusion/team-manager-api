export type ProfessionalProfileEntity = {
  id: string;
  platform: string;
  url: string;
  memberId: string;
  creadtAt: string;
};

export type HttpProfessionalProfileEntity = ProfessionalProfileEntity;
